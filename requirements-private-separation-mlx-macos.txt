# Optional private-development runtime for same-checkpoint Demucs parity.
#
# This file is deliberately separate from requirements-ai-macos.txt.  It must
# not be installed as part of ordinary Sunofriend setup and does not enable a
# separator in the CLI, TUI, Simple mode or Studio.  The parity runner converts
# only the already-installed, hash-pinned PyTorch checkpoint in memory; it
# never calls demucs-mlx's named-model downloader or writes an MLX weight cache.
#
# Code licences checked 2026-07-31:
#   demucs-mlx, MLX, mlx-metal, mlx-audio-io and mlx-spectro: MIT
demucs-mlx==1.4.4
mlx==0.31.2
mlx-audio-io==1.3.11
mlx-spectro==0.7.0
