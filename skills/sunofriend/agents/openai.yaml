interface:
  display_name: "Sunofriend"
  short_description: "Guide stems to reviewed MIDI and a song WAV"
  default_prompt: "Use $sunofriend. Prefer the Guided Local Studio TUI through `sunofriend tui` when I want to operate the project myself; keep the deterministic CLI as the engine, and use the loopback Workbench for graphical multi-process MIDI comparison, explicit choices, the balanced MIDI-derived song-interpretation WAV and the separate exact GarageBand MIDI ZIP. Source stems are evidence and are not mixed into the interpretation WAV. Use reviewed immutable Clip workflows only behind their existing gates. Keep audio local and do not infer preference from playback, scores or visible defaults."
